import React from 'react';
import ReactDOM from 'react-dom/client';

const rootDOM = document.getElementById('root');
const appRoot = ReactDOM.createRoot(rootDOM);

appRoot.render(
    <h1>Push to make a pull request</h1>
);